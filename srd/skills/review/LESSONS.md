# Lessons — review

- Never quote literal doubled or trailing whitespace as a finding's evidence;
  describe it in words ("two consecutive spaces before the word 'in'"). Wrapping
  the review file normalizes whitespace and silently destroys the quoted proof.
