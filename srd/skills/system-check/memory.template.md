# System Memory

Curated knowledge base for `system-check`. One atomic fact per line, grouped
by topic. Each line ends with its source in brackets, relative to the space root
below; `[unwritten]` marks tribal facts in no doc, `[same]` repeats the line
above. The skill maintains this file; it grows as SRD reviews surface facts.

Space root: `<unset>` — set this to your local documentation root. Leave it
`<unset>` if you have no space docs; the skill then runs without the live-doc
layer and only the facts below are used.

## Contents

<!-- One "## Topic" section per area, listed here. Empty on a fresh checkout. -->

<!--
Example shape (delete once you add real facts):

## System Overview

- The platform is a single Go monolith behind one HTTP gateway. [Overview.md]
-->
